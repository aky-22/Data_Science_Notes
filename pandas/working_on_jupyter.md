# Working on Jupyter

```sh
source .venv/bin/activate
jupyter notebook
```

This activates a python environment which is called `.venv`.
You can deactivate it if you want: `deactivate`

To check whether you are in that environment:

```sh
which python
/home/yeak/Playground/data_science/.venv/bin/python
```
